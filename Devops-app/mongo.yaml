version: '3.8'

services:
  mongodb:
    image: mongo
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=password
    volumes:
      - mongodb_data:/data/db

  mongo-express:
    image: mongo-express
    ports:
      - "8081:8081"
    environment:
      - ME_CONFIG_MONGODB_ADMINUSERNAME=admin
      - ME_CONFIG_MONGODB_ADMINPASSWORD=password
      - ME_CONFIG_BASICAUTH_USERNAME=zacks
      - ME_CONFIG_BASICAUTH_PASSWORD=devops
      - ME_CONFIG_MONGODB_SERVER=mongodb
    depends_on:
      - mongodb

# my node.js app that need mongo and mongo-express

 # asset-tracker:
 #   image: 300403499943.dkr.ecr.us-east-1.amazonaws.com/devops_app:v1
 #   ports:
 #     - "3000:3000"
 #   environment:
 #     - MONGO_URI=mongodb://admin:password@mongodb:27017/assetdb?authSource=admin
 #   depends_on:
 #     - mongodb
volumes:
  mongodb_data:
    driver: local 